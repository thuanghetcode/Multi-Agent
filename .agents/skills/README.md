# Project GPT skills

The portable Agent Skills are under `.agent-skills/`. Continue uses the adapters in `.continue/rules/` and `.continue/prompts/` because its native configuration is based on rules and prompts.
