# Project profile

This template uses the `lean` profile:

- `director`: GPT through Continue
- `executor`: Claude Code
- independent reviewer: human/GPT review step
- subagents and agent teams: disabled by default

For task-specific routing, use `docs/PROFILES.md`.
