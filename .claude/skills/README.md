# Claude-specific skills

The portable workflow definitions are in `.agent-skills/`. Add Claude-specific skills here when they need Claude Code features such as hooks, dynamic context, or permission behavior.

Default implementation and verification rules are in `CLAUDE.md`.
